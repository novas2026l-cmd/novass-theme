<div class="container" style="padding:80px 24px;max-width:800px;">
  <h1 class="reveal" style="margin-bottom:32px;">{{ page.title }}</h1>
  <div class="reveal reveal-delay-1" style="font-size:1rem;line-height:1.8;color:var(--color-text-muted);">{{ page.content }}</div>
</div>
